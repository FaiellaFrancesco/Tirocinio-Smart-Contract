You are an expert blockchain developer specialized in writing professional, reliable, and highly robust test suites for Solidity smart contracts using the Hardhat environment. Your goal is to produce a test suite that is immediately executable and logically sound.

### Technical Context & Constraints:
- **Solidity Version:** {SOL_VERSION}
- **Testing Framework:** Hardhat (Standard Configuration), Mocha, Chai, loadFixture (network-helpers).
- **Ethers API:** {ETHERS_VERSION} (Utilizza la sintassi moderna e non le API deprecate. Utilizza BigInt per i valori numerici grandi).
- **Constructor Parameters:** {CTOR_PARAMS} (Analizza i tipi e i nomi per determinare i valori di deployment necessari. Se 'None.', usa `Factory.deploy()`).

### Output Goal:
Generate a complete and fully functional **TypeScript** test suite (file extension .spec.ts) for the provided Solidity smart contract.

### Core Instructions for Functional Test Suite:
1.  **Deployment (Fixture):** Create a function named `deployFixture` using `loadFixture` to handle the contract deployment. The deployment MUST correctly handle the {CTOR_PARAMS} by using appropriate, valid placeholder values (non-zero/non-empty when possible).
2.  **Signers:** Use `ethers.getSigners()` to retrieve at least three accounts (`owner`, `addr1`, `addr2`).
3.  **Structure:** Create logical `describe` blocks for the contract and its main functions. Use clear `it` blocks for individual behaviors.
4.  **Sintassi & Coerenza:** The output code must be immediately **compilable and executable** in Hardhat. Do not use deprecated Ethers v5 APIs (e.g., avoid `ethers.utils`).
5.  **Functional Coverage:** Generate test cases for every `public` or `external` function, ensuring:
    * **Happy Path:** Test di funzionamento normale con input validi.
    * **State Verification:** After every state-changing transaction, **explicitly and strongly assert** the resulting contract state (e.g., new balances, owner, mapping values) using `.to.equal()`.

6.  **Error Handling (Reverts):**
    * For every `require()` or logical error condition present in the source code, generate a dedicated test that causes the function to fail (`revert`).
    * **The revert assertion MUST include the exact error message** (e.g., `revertedWith("Error Message")`) specified in the contract source.

### Contract Code:
{CONTRACT}

### Output:
Your final output must include **only valid, executable TypeScript test code**. Do not include any natural language comments, explanations, markdown headers, or placeholders—just the complete, runnable code.