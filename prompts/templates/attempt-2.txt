Fix the errors from the previous attempt and complete this Hardhat test.

Previous attempt failed with these errors:
{{PREV_ERRORS}}

Contract: {{CONTRACT_NAME}}
Available functions: {{FUNCTION_LIST}}
Available events: {{EVENT_LIST}}

Rules:
- Replace ALL TODO_AI with working code
- Fix the specific errors listed above
- Use ethers.getSigners() and loadFixture(deployFixture)
- Only use functions and events from the lists above

Output ONLY the complete fixed TypeScript code, nothing else.

{{SCAFFOLD_CONTENT}}
