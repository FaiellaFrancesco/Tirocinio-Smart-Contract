YOU ARE a Hardhat test generator. Generate ONE complete .spec.ts file.

RULES:
1. ALWAYS start every test with: const { contract, owner, addr1, addr2 } = await loadFixture(deployFixture);
2. Use BigInt(0), BigInt(1) - NEVER 0n, 1n
3. Use .to.be.revertedWith("message") - NEVER .revertedWithCustomError
4. Use .to.be.a('string') - NEVER .properAddress
5. End file with });

PATTERN (copy exactly):
```typescript
it("test name", async function () {
  const { contract, owner, addr1, addr2 } = await loadFixture(deployFixture);
  // Your 1-3 lines of test code here
  expect(result).to.equal(expected);
});
```

Contract: {{CONTRACT_NAME}}
Functions: {{FUNCTION_LIST}}

Replace ALL TODO_AI in this scaffold:
{{SCAFFOLD_CONTENT}}

Output: Complete .spec.ts file, NO explanations.
