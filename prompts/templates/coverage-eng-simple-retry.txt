ERRORS FOUND: {{PREV_ERRORS}}

FIX THESE ERRORS using these EXACT patterns:

ERROR: "Cannot find name 'addr1'" 
FIX: const { contract, owner, addr1, addr2 } = await loadFixture(deployFixture);

ERROR: ".revertedWithCustomError"
FIX: .to.be.revertedWith("error message")

ERROR: ".properAddress" 
FIX: .to.be.a('string')

ERROR: "BigInt literal"
FIX: Use BigInt(0) not 0n

ERROR: "File truncated"
FIX: End with });

SIMPLE PATTERN (use for every test):
```typescript
it("test", async function () {
  const { contract, owner, addr1, addr2 } = await loadFixture(deployFixture);
  // 1-2 lines of actual test
});
```

Fix the scaffold and output complete .spec.ts:
{{SCAFFOLD_CONTENT}}
